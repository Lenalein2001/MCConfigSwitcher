Place a multi-size ICO at this path to brand the executable:

  Assets\app.ico

Recommended sizes in the single .ico container: 16x16, 32x32, 48x48, 64x64, 256x256 (all at 32-bit RGBA). 

After adding the file, rebuild the project and the EXE will use it automatically.
